// import { EventBus, ServerEvent } from "../src/EventBus";
// import { Game } from "../src/Game"

// describe('test', () => {
//     test('test 1', () => {
//         const eventBus = new EventBus();
//         eventBus.listen(ServerEvent.map, {}, (a) => {
//             expect(a.length).toBeTruthy();
//         })
//         const game = new Game(eventBus);
//     })
// })